name: Live Nepal News Sync

on:
  workflow_dispatch:
  schedule:
    - cron: "*/15 * * * *"

permissions:
  contents: write

concurrency:
  group: live-news-sync
  cancel-in-progress: true

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Fetch and normalize news
        run: python scripts/fetch_news.py

      - name: Publish updated news data
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: "chore: sync live Nepal news"
          file_pattern: "data/news.json data/feed-health.json"
