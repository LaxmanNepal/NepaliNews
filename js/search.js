import{getNews,normalize}from"./news-engine.js";const q=new URLSearchParams(location.search).get("q")||"";const root=document.querySelector("#content");async function run(){const items=normalize(await getNews());const results=items.filter(x=>(x.title+" "+x.summary+" "+x.source).toLowerCase().includes(q.toLowerCase()));if(root)root.innerHTML=results.slice(0,60).map(x=>'<article class="story"><div class="meta">'+x.source+'</div><h3>'+x.title+'</h3><p>'+x.summary+'</p><div class="source"><span>'+new Date(x.date).toLocaleString()+'</span><a href="'+x.link+'" target="_blank" rel="noopener">Read original →</a></div></article>').join("")||'<div class="empty">No matching stories found.</div>'}run();