export function normalizeTitle(t){return String(t||"").toLowerCase().replace(/https?:\\/\\/\\S+/g,"").replace(/[^\\p{L}\\p{N} ]/gu," ").replace(/\\s+/g," ").trim()}export function similarity(a,b){const A=new Set(normalizeTitle(a).split(" ")),B=new Set(normalizeTitle(b).split(" "));if(!A.size||!B.size)return 0;let hit=0;for(const x of A)if(B.has(x))hit++;return hit/Math.max(A.size,B.size)}export function cluster(items,threshold=.55){const out=[];for(const item of items){let found=out.find(g=>similarity(g[0].title,item.title)>=threshold);if(found)found.push(item);else out.push([item])}return out}